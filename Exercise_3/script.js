
let isSunny = true;
let isWeekend = false;
console.log(isSunny && isWeekend); // Output: false (both conditions must be true to go to the beach)
console.log(isSunny || isWeekend); // Output: true (either condition can be true)
console.log(!isSunny);             // Output: false (inverts the boolean value)
